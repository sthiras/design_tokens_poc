import { Token } from "../../..";
export declare type DTProcessedTheme = {
    sets: Array<DTProcessedTokenSet>;
};
export declare type DTProcessedTokenSet = {
    tokens: Array<Token>;
};
/** Utility allowing merging of two pools of tokens */
export declare class DTTokenSetResolver {
    constructor();
    parseTokenSets(sets: any): void;
}
